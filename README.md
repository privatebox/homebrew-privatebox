# PrivateBox Homebrew tap

Install the PrivateBox CLI on macOS or Linux:

```bash
brew tap privatebox/privatebox
brew install --cask privatebox
```

The cask supports AMD64 and ARM64. It is generated from verified release
archives and updated automatically by the privatebox-cli release workflow.
